# Supabase Configuration
# Get these from your Supabase Project Settings > API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# Authentication (Google SSO)
# Required for native Google Sign-In
GOOGLE_WEB_CLIENT_ID=your-web-client-id.apps.googleusercontent.com
GOOGLE_IOS_CLIENT_ID=your-ios-client-id.apps.googleusercontent.com

# OAuth Redirect URLs
# Configure these in Supabase Dashboard > Authentication > URL Configuration
# For development (local testing)
REDIRECT_URL_DEV=io.supabase.perfumegpt://login-callback
# For production (deployed app)
REDIRECT_URL_PROD=io.supabase.perfumegpt://login-callback
# Deep link scheme for mobile app
DEEP_LINK_SCHEME=perfumegpt

# AI Configuration (If used directly in mobile, though usually through web/backend)
XAI_API_KEY=your-xai-api-key

# Note: Assets/.env must be added to assets section in pubspec.yaml

# Facebook Configuration
FACEBOOK_APP_ID=your-facebook-app-id
FACEBOOK_CLIENT_TOKEN=your-facebook-client-token
